# Kmeans for C language 


This programs implements the K-means algorithm in C language for any number of clusters. You can check the screenshots "Unlabeled" and "Labeled" for before and after data clustering.
